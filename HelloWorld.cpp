#include <iostream>

//void Log(const char* message);

int main()  // we dont need to return an integer for main function and
			// it automatically assumes it returns 0. but only for main func!
{
	std::cout << "Hello, world!"<< std::endl;
	std::cin.get(); // the programm waits for us untill we press Enter
  
}

